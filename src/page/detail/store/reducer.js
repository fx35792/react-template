import {fromJS} from 'immutable';

const defaultState = {
    content: '11212',
};

export default (state = defaultState, action) => {
    switch (action.type) {
        default:
            return state
    }

}